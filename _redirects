# Proxy the API through this site's own origin → the browser sees one origin, no CORS.
# (Netlify / Cloudflare Pages read this file. "200" = rewrite/proxy, not a redirect.)
/api/*  https://zawathon.ascentcloud.art/api/:splat  200
